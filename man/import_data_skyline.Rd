% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-import.R
\name{import_data_skyline}
\alias{import_data_skyline}
\title{Import Skyline Peak Integration Results}
\usage{
import_data_skyline(
  data = NULL,
  path,
  transition_id_columns = c("name", "mz", "none"),
  import_metadata = TRUE,
  silent = FALSE
)
}
\arguments{
\item{data}{A \code{MidarExperiment} object.}

\item{path}{One or more file paths, or a directory path from which all
matching files will be imported.}

\item{transition_id_columns}{A character vector specifying the columns that
define the transition (precursor and product) to use for unique \code{feature_id}
generation. Options are "name", "mz", or "none". If "none", \code{feature_id} is
derived from \verb{Molecule Name} and \verb{Precursor Name} or \verb{Product Name}. If "mz",
\code{feature_id} is based on \verb{Precursor Mz} and \verb{Product Mz}. Using "none" will
result in \code{feature_id} being a copy of \verb{Molecule Name}, with an error raised
if it is not unique for each transition.}

\item{import_metadata}{Logical; whether to import additional metadata columns
(e.g., precursor/product m/z values).}

\item{silent}{Logical; whether to suppress most notifications.}
}
\value{
A \code{MidarExperiment} object containing the imported data.
}
\description{
This function imports tabular data files (*.csv) exported from \code{Skyline},
containing peak integration results.
}
\details{
In Skyline, transitions are defined by
the \verb{Molecule Name} and corresponding precursor and product m/z values,
rather than any identifier. When importing this data, the \code{feature_id} is
generated using the \verb{Molecule Name} with either precursor/product names or
m/z values, unless \verb{Molecule Name} uniquely identifies the features (refer to
the \code{transition_id_columns} argument below).

The following supported columns from Skyline can be imported:\tabular{lll}{
   Skyline Column Name \tab MiDAR Column Name \tab Required \cr
   \verb{Replicate Name} \tab \code{analysis_id} \tab Yes \cr
   \verb{Molecule List Name} \tab \code{feature_class} \tab No \cr
   \verb{Molecule Name} \tab \code{feature_id} \tab Yes \cr
   \verb{Precursor Name} \tab \code{feature_id} \tab Yes* \cr
   \verb{Product Name} \tab \code{feature_id} \tab Yes* \cr
   \verb{Precursor Mz} \tab \code{method_precursor_mz} \tab Yes* \cr
   \verb{Product Mz} \tab \code{method_product_mz} \tab Yes* \cr
   \code{Area} \tab \code{feature_area} \tab Yes \cr
   \verb{Retention Time} \tab \code{feature_rt} \tab No \cr
}


*Requirements for these columns are described in \code{transition_id_columns}.

To export results from Skyline, use the 'Molecule Transition Results' format
and include \verb{Replicate Name}, \verb{Molecule Name}, and either
\verb{Precursor Mz}/\verb{Product Mz} or \verb{Precursor Name}/\verb{Product Name} columns.
At least one feature variable, such as \code{Area} or \code{RT}, must also be exported.
}
\examples{
mexp <- MidarExperiment()
file_path <- system.file("extdata", "Skyline_MoleculeTransitionResults.csv", package = "midar")
mexp <- import_data_skyline(
  data = mexp,
  path = file_path,
  transition_id_columns = "mz",
  import_metadata = TRUE
)
print(mexp)
}
